import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.urolinq.pro',
  appName: 'UROLINQ Pro',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
